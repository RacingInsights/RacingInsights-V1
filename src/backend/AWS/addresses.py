from enum import Enum

class AWSAddress(Enum):
    """Contains the specific addresses for connecting to the AWS clients/resources/services"""
    APP_CLIENT_ID = '************'
    REGION = '*******************'
    AWS_ACCOUNT_ID = '***********'
    IDENTITY_POOL_ID = '*********'
    USER_POOL_ID = '*************'
